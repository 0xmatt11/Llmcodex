services:
  bridge:
    build: .
    restart: unless-stopped
    env_file: .env
    shm_size: "1gb"
    ports:
      - "3000:3000"
    volumes:
      - bridge-data:/app/data
      - bridge-browser-data:/app/browser-data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3000/healthz || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 45s

volumes:
  bridge-data:
  bridge-browser-data:
